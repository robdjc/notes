Command used for checking and chaning terminal settings:

List the current terminal settings:

`stty -a`


Add/Remove settings:

`stty -echo`/`stty echo`


Use a reset command to get the terminal back to normal:

`reset`

